Name=sub
Owns=condition
||
  if AName = '|name|' then
|IF Owns|
    RestoreYamlObjectOwn(F|Name|, StrToInteger(AValue))
|ELSE Owns|
    RestoreYamlObjectRef(F|Name|, StrToInteger(AValue))
|ENDIF Owns|
  else